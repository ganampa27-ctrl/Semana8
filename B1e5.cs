using UnityEngine;

public class B1e5 : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        for (int i = 5; i >= 0; i--)
        {       
            print("Inicia la ronda en " + i);
        }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
